import React from 'react';
import './styles.css';

const Header = () => (
    //return () é opcional quando não existe nenhum código antes do return
    <header id="main-header">JSHunt</header>
);

export default Header;